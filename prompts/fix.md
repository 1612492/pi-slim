---
description: Use the fixer subagent to implement a scoped change in isolated context.
---

Use the `subagent` tool in single mode with `agent: "fixer"` to implement: $@

Ask fixer to keep changes scoped and include concise verification notes.
