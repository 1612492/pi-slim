---
description: Use the explorer subagent for focused local code discovery and return a compact handoff.
---

Use the `subagent` tool in single mode with `agent: "explorer"` to investigate: $@

Keep the task narrow and return the explorer's structured findings.
